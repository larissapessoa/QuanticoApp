import { ModelsDirective } from './models.directive';

describe('ModelsDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelsDirective();
    expect(directive).toBeTruthy();
  });
});
